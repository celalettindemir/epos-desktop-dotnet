<UserControl x:Class="EposWpf.View.PortionItemRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EposWpf.View"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Button x:Name="LittleBtn" BorderThickness="0" Grid.Column="0" FontSize="16" Content="Little"  Height="38" Margin="5 3"/>
        <Button x:Name="MediumBtn" BorderThickness="0"  Uid="{Binding _uid}" Grid.Column="1" FontSize="16" Content="{Binding _content}" Height="38" Margin="5 3"/>
        <Button x:Name="ExtraBtn" BorderThickness="0"  Grid.Column="2" FontSize="16" Content="Extra"  Height="38" Margin="5 3"/>
    </Grid>
</UserControl>
